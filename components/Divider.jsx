import { View } from "react-native";

const Divider = () => {
  return (
    <View
      style={{
        borderBottomWidth: 0.5,
        borderBottomColor: "#808080",
        width: "100%",
      }}
    />
  );
};

export default Divider;
